<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="
	     	http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
	     	http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
           	http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.0.xsd          
			http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.0.xsd"
	default-lazy-init="true">
	
	<!-- 注解支持 -->
	<!-- <context:annotation-config /> -->
	
	<!-- 启动spring组件扫描，排除@Controller组件（该组件由SpringMVC配置文件扫描） -->
	<context:component-scan base-package="com.deertt">
		<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller" />
	</context:component-scan>
	
	<!--用于读取.properties配置文件的信息 -->
	<bean id="propertyConfigurer" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="locations">
			<list>
				<value>classpath:config/db/jdbc.properties</value>
			</list>
		</property>
	</bean>
	
	<!--定义数据源 -->
	<bean id="dataSource" class="org.logicalcobwebs.proxool.ProxoolDataSource" lazy-init="true">
		<property name="driver" value="${jdbc.driverClassName}" />
		<property name="driverUrl" value="${jdbc.url}" />
		<property name="user" value="${jdbc.username}" />
		<property name="password" value="${jdbc.password}" />
		<property name="alias" value="${jdbc.alias}" />
		<property name="minimumConnectionCount" value="${jdbc.minimumConnectionCount}" />
		<property name="maximumConnectionCount" value="${jdbc.maximumConnectionCount}" />
		<!-- 
		<property name="houseKeepingSleepTime" value="${jdbc.houseKeepingSleepTime}" />
		<property name="maximumActiveTime" value="${jdbc.maximumActiveTime}" />
		<property name="maximumConnectionLifetime" value="${jdbc.maximumConnectionLifetime}" />
		<property name="overloadWithoutRefusalLifetime" value="${jdbc.overloadWithoutRefusalLifetime}" />
		<property name="recentlyStartedThreshold" value="${jdbc.recentlyStartedThreshold}" />
		 -->
		<property name="houseKeepingTestSql" value="${jdbc.houseKeepingTestSql}" />
		<property name="prototypeCount" value="${jdbc.prototypeCount}" />
		<property name="simultaneousBuildThrottle" value="${jdbc.simultaneousBuildThrottle}" />
		<property name="statistics" value="${jdbc.statistics}" />
		<property name="testBeforeUse" value="${jdbc.testBeforeUse}" />
		<property name="testAfterUse" value="${jdbc.testAfterUse}" />
		<property name="trace" value="${jdbc.trace}" />
		<property name="verbose" value="${jdbc.verbose}" />
	</bean>
	
	<!--定义jdbcTemplate -->
	<bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource"/>
	</bean>
	
	<!--定义事务管理器 -->
	<bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource"/>
	</bean>
	
	<!-- 自动扫描@Transaction事务 -->
	<tx:annotation-driven transaction-manager="transactionManager"/>
 
	<!-- 用于持有ApplicationContext,可以使用SpringContextHolder.getBean('xxxx')的静态方法得到spring bean对象 -->
	<bean class="com.deertt.frame.base.project.SpringContextHolder" lazy-init="false" />

</beans>
